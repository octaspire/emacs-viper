#!/bin/sh
echo "------------------------------------------------------------"
echo "Makefile for managing Octaspire's Emacs Viper configuration"
echo "------------------------------------------------------------"
echo "Targets are:"
echo ""
echo "all    - the default target, same as link."
echo "link   - create symbolic links to user's home directory."
echo "unlink - remove the symbolic link from user's home directory."
echo "test   - compare user's version of configuration to the repository's one."
echo "clean  - remove all generated and installed files (also packages, if installed)."
echo "help   - run this target; list available targets."

